{
  "name": "chijs-collection",
  "version": "1.0.0",
  "private": true,
  "description": "CHI J'S Collection — Single-vendor fashion storefront and admin portal",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "prisma:studio": "npm run prisma:studio --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
